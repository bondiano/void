### void/core/plugin — plugin API (SPEC.md §3.4, part II).
### Plugin = package + manifest (frozen struct). Extension points with
### schemas, cardinality, and reduce strategies; (plugin/inspect), dry-run.
