### void/core/config — layered configuration (SPEC.md §3.2).
### plugin defaults <- config files <- env vars <- CLI overrides,
### with per-value provenance ((config/explain ...)), profiles, secrets.
