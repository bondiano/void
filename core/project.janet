(declare-project
  :name "void-core"
  :description "void/core — component system, config, schema layer, plugin API, hooks. The only mandatory dependency of a void application."
  :version "0.0.1"
  :dependencies ["https://github.com/janet-lang/spork.git"])

(declare-source
  :source ["void"])
