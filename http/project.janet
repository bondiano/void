(declare-project
  :name "void-http"
  :description "void/http — HTTP kernel: server on net/ev, router, route metadata (SPEC §5.1). Wire-format primitives inherited from spork/http (ADR-0015)."
  :version "0.0.1")

(declare-source
  :source ["void"])
